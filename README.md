# zcli.config.apps.json parameters settings parses differently by zcli apps:update/zendesk settings UI and zcli apps:server

Reproduce issue how ZCLI and ZAT parses setting objects differently in Zendesk.